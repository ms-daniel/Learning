class operations:
    def soma(self, x, y):
        return float(x)+float(y)

    def diferenca(self, x,y):
        return float(x)-float(y)

    def multiplicacao(self, x,y):
        return float(x)*float(y)

    def divisao(self, x,y):
        return float(x)/float(y)